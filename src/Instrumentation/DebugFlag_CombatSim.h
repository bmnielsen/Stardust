#pragma once

#if INSTRUMENTATION_ENABLED
#define DEBUG_COMBATSIM true        // Draws each sim result and writes log messages for interesting transitions
#define DEBUG_COMBATSIM_LOG true   // Writes log messages for each sim result
#endif
