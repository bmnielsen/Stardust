#pragma once

#if INSTRUMENTATION_ENABLED_VERBOSE
#define DEBUG_UNIT_ORDERS true   // Writes a log message for each order sent to a unit
#define DEBUG_UNIT_BOIDS  true  // Writes a log message for all boids
#endif
